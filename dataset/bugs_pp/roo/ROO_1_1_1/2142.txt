dbre fkey type table referenced table
running command roo commandreverse engineer schema konakart domaini domain error message type table roots referenced table products this happening mysql sql database konkart dbre processed error prior adding foreign key constraints ddl products table wich products custom custom foreign key constraint mysql sql ddl productscreate products create table products products auto increment products quantity products model varchar products image varchar products price decimal products datetime products modified datetime products datetime products weight decimal products status tinyint products tax manufacturers products ordered custom custom custom custom varchar custom custom varchar custom custom varchar custom custom varchar products invisible products sku varchar products type products file path varchar products content type varchar products image image varchar products image image varchar products image image varchar products price decimal products price decimal products price decimal primary key products unique key idx products sku products sku key idx products products key idx products manufacturers manufacturers key idx products custom custom custom custom constraint products custom custom foreign key custom custom references roots roots engine innodb inno auto increment charset utf here utf ddl roots table target products custom custom constraint mysql sql ddl rootscreate roots create table roots roots auto increment roots varchar roots desc mediumtext roots varchar roots synopsis varchar primary key roots engine innodb inno auto increment charset utf this utf issue posted camelcase camel usage table names products roots table elements products roots database xml element generated database introspect schema konakart introspect schema table products column products primarykey primary key required size type column products quantity primarykey primary key required size type column products model primarykey primary key required size type varchar column products image primarykey primary key required size type varchar column products price primarykey primary key required size type decimal column products primarykey primary key required size type datetime column products modified primarykey primary key required size type datetime column products primarykey primary key required size type datetime column products weight primarykey primary key required size type decimal column products status primarykey primary key required size type bit column products tax primarykey primary key required size type column manufacturers primarykey primary key required size type column products ordered primarykey primary key required size type column custom custom primarykey primary key required size type column custom custom primarykey primary key required size type varchar column custom custom primarykey primary key required size type varchar column custom custom primarykey primary key required size type varchar column custom custom primarykey primary key required size type varchar column products invisible primarykey primary key required size type column products sku primarykey primary key required size type varchar column products type primarykey primary key required size type column products file path primarykey primary key required size type varchar column products content type primarykey primary key required size type varchar column products image image primarykey primary key required size type varchar column products image image primarykey primary key required size type varchar column products image image primarykey primary key required size type varchar column products price primarykey primary key required size type decimal column products price primarykey primary key required size type decimal column products price primarykey primary key required size type decimal foreign key foreigntable foreign table roots products custom custom ondelete delete onupdate update option key exported reference foreign roots local custom custom foreign key foreign key foreigntable foreign table orders products ordersproducts orders products productsid products ondelete delete onupdate update option key exported reference foreign products local products foreign key unique primary unique column products unique unique idx products sku unique column products sku unique idx products column products idx products manufacturers column manufacturers idx products custom custom column custom custom table table roots column roots primarykey primary key required size type column roots primarykey primary key required size type varchar column roots desc primarykey primary key required size type mediumtext column roots primarykey primary key required size type varchar column roots synopsis primarykey primary key required size type varchar foreign key foreigntable foreign table products products custom custom ondelete delete onupdate update option key exported reference foreign custom custom local roots foreign key unique primary unique column roots unique table good